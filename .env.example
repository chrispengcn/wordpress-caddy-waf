# ============================================================
# WordPress + FrankenPHP + Coraza WAF 环境变量配置
# ============================================================

# -----------------------------------------------------------------------------
# 数据库配置 (MariaDB)
# -----------------------------------------------------------------------------
MYSQL_ROOT_PASSWORD=change-this-root-password-2024
MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress
MYSQL_PASSWORD=change-this-wordpress-password

# -----------------------------------------------------------------------------
# WordPress 数据库连接
# 与上面 MariaDB 配置保持一致
# -----------------------------------------------------------------------------
WORDPRESS_DB_HOST=db:3306
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=change-this-wordpress-password

# -----------------------------------------------------------------------------
# imgproxy 签名密钥
# 生成命令: openssl rand -hex 16
# deploy.sh 首次运行会自动生成
# -----------------------------------------------------------------------------
IMGPROXY_KEY=0123456789abcdef0123456789abcdef
IMGPROXY_SALT=fedcba9876543210fedcba9876543210

# -----------------------------------------------------------------------------
# 可选: 你的域名
# 如果设置域名，Caddy 会自动申请 SSL 证书
# 取消注释并修改 Caddyfile 中的 auto_https
# -----------------------------------------------------------------------------
# SERVER_NAME=your-domain.com,www.your-domain.com
